---
import BaseLayout from "../../layouts/BaseLayout.astro";
import SolutionsBanner from "../../components/SolutionsBanner.astro";

const pythonSolutions = await Astro.glob("../solutions/python/*.md");
---

<BaseLayout pageTitle={"Leetcode Solutions | Solutions"}>
  <SolutionsBanner />
  <div class="text-center px-4 md:px-40 text-lg text-black dark:text-white py-8">
    <h2 class="py-3 font-semibold">
      In this page, we can find all the Leetcode solutions in <span
        class="whitespace-nowrap text-blue-600 font-bold dark:text-orange-600">python/ Javascript.</span
      >
    </h2>
    <h4 class="font-semibold">
      These solutions are added from <span class="whitespace-nowrap text-blue-600 font-bold dark:text-orange-600"
        >October 31'st 2022 📅
      </span>
    </h4>
  </div>

  <div class="px-4 md:px-64 py-3 md:py-6">
    <h2 class="text-2xl text-center text-blue-600 font-bold dark:text-orange-600">
      Below Leetcode solutions are in python language, order by created date ⚡
    </h2>
  </div>

  <div class="px-6 md:px-44 mt-8 flex flex-col justify-center items-center">
    {
      pythonSolutions.map((solution) => {
        return (
          <a
            href={solution.url}
            class="bg-slate-300 dark:bg-rose-800 w-full py-3 my-3 rounded-xl text-center text-blue-900 dark:text-white font-semibold text-lg"
          >
            <div>{solution.frontmatter.title}</div>
          </a>
        );
      })
    }
  </div>
</BaseLayout>
